extends layout

block content
  h1= title
  p Creating for Lab 10
  button(type='button' class='btn btn-info' onClick="update()") Press Me
  div(id="target") Display
  script.
    var client_count=0;
    function update(){
      client_count=client_count+1;
      document.getElementById("target").innerHTML=`Value now ${client_count}`;
    }
  a(href='bat') Bat